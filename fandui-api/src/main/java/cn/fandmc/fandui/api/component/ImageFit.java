package cn.fandmc.fandui.api.component;

/** Controls how an image is scaled into its measured component bounds. */
public enum ImageFit {
    FILL,
    CONTAIN,
    COVER,
    NONE,
    SCALE_DOWN
}
